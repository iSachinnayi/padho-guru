name: padho_guru_server
description: पढ़ो Guru - Genkit AI Server for Cloud Run
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  genkit: ^0.5.0
  genkit_openai: ^0.5.0
  genkit_shelf: ^0.5.0
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  schemantic: ^0.4.0
  firebase_admin: ^0.2.0
  dotenv: ^4.1.0

dev_dependencies:
  build_runner: ^2.4.0
  lints: ^5.0.0
